{
  "name": "dusty-bot",
  "version": "1.0.0",
  "private": true,
  "description": "2D roguelike robot vacuum — static site, no build step. Tests only.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
